{
  "schemaVersion": 1,
  "id": "raglite",
  "name": "RAGLite",
  "description": "Local-first RAG cache: distill docs into structured Markdown, then index/query with Chroma + hybrid search.",
  "version": "1.0.7",
  "entrypoints": {
    "run": {
      "kind": "exec",
      "command": [
        "bash",
        "-lc",
        "${SKILL_DIR}/scripts/raglite.sh $@"
      ],
      "args": [
        "--"
      ]
    }
  }
}
